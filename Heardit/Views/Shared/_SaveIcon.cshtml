@model bool
@* A bookmark, hollow until the track is in the queue. Shared by the feed cards and the song page. *@

<svg width="16" height="16" viewBox="0 0 24 24" fill="@(Model ? "currentColor" : "none")" aria-hidden="true" focusable="false">
    <path d="M6 4.5h12v15l-6-4.2-6 4.2z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round" />
</svg>
